import { RegisterScreen } from '../src/features/register/components/RegisterScreen'; // Asumiendo que RegisterScreen es un componente independiente

export default function RegisterRoute() {
  // Aquí podrías pasar props o manejar eventos de éxito, pero la navegación a esta pantalla ya está manejada por expo-router
  return <RegisterScreen />;
}